'use strict';

/**
 * healthcare-provider controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::healthcare-provider.healthcare-provider');
