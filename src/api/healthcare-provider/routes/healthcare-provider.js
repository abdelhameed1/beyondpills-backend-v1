'use strict';

/**
 * healthcare-provider router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::healthcare-provider.healthcare-provider');
